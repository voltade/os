{
  "private": true,
  "name": "@voltade/runner",
  "license": "AGPL-3.0-only",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server/index.ts",
    "build": "bun build --minify --sourcemap --target=bun --outdir=dist server/index.ts",
    "docker": "docker build -t registry.127.0.0.1.nip.io/runner:latest . && docker image push registry.127.0.0.1.nip.io/runner:latest"
  },
  "devDependencies": {
    "@hono/zod-validator": "^0.7.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.9.0",
    "tar": "^7.4.3",
    "zod": "^4.0.15"
  }
}
