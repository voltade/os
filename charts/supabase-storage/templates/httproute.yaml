{{- if .Values.httproute.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "supabase-storage.fullname" . }}
  labels:
    {{- include "supabase-storage.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ .Values.global.gatewayName }}
      namespace: {{ .Values.global.gatewayNamespace }}
  hostnames:
    - "{{ include "supabase-storage.fullname" . }}.{{ .Release.Name }}.{{ .Values.global.baseDomain }}"
  rules:
    - backendRefs:
        - name: {{ include "supabase-storage.fullname" . }}
          port: {{ .Values.service.port }}
      matches:
        - path:
            type: PathPrefix
            value: /
{{- end }}
